# 調達画面 (Procurement) パフォーマンス最適化計画

本計画は、在庫画面（`inventory.js`, `inventory_mobile.js`）で実施した処理速度改善の仕組みを、調達画面（`procurement.js`, `procurement_mobile.js`）へ横展開し、同様の最速パフォーマンスを実現するための実装手順です。

## 課題の背景と目的
現在、調達画面（仕入れ、店舗仕込み、CK仕込み等）において、以下の2つのボトルネックが存在します。

1. **直列通信による処理の遅延**: 「仕込み完了」や「仕入れ完了」などのアクション時、Firestoreに対して `updateDoc` と `addDoc` が別々に（awaitで直列に）実行されており、通信回数が多くフリーズの要因となっています。
2. **リアルタイムリスナーによる全再描画（二重描画）**: アクション完了時に自身の書き込みがFirestoreから返ってきた際（`onSnapshot` 発火時）、自分が書き込んだにもかかわらず画面全体が `innerHTML` でフルリセット（全再描画）されてしまいます。

本計画では、これらを「`writeBatch`を用いた1回通信」と「ピンポイントでのDOM更新」に改修し、ボタン押下直後に待ち時間ゼロでUIがサクサク反応する仕様に変更します。

---

## 提案する修正内容

以下の修正を、PC版 (`procurement.js`) および スマホ版 (`procurement_mobile.js`) の双方に適用します。

### 1. 通信の `writeBatch` 化（アトミックな1回通信への統合）
対象となる複数の関数内の通信ロジックを `writeBatch` に書き換えます。
これにより、在庫数（`m_store_items`）の更新、履歴（`t_inventory_history`）の追加、仕込みリクエスト（`t_prep_requests`）等の更新処理を1回の通信にまとめます。

**主な改修対象関数:**
- `executeSingleAction` (または `executePrepAction` 等の仕込み実行関数)
- `executeSinglePurchaseAction` (不足分アクションからの仕入れ処理)
- `saveItemQty` または `processManualAction` (直接入力による実行関数)
- `executeBatchPrepAction` (一括実行機能。既存の `writeBatch` 実装をより堅牢に)

### 2. `onSnapshot` の最適化（二重描画の防止）
在庫画面と同様に、データのリアルタイムリスナーに自身の書き込みを判定するガードを追加します。

**変更内容:**
- `onSnapshot(q, { includeMetadataChanges: true }, (snap) => { ... })` に変更。
- コールバック内で `if (!snap.metadata.hasPendingWrites)` の条件判定を設け、他者の更新やサーバー側の確定時のみ `render()` を発火させるように制御します。

### 3. アクション実行時のピンポイントDOM更新（レンダリングの軽量化）
ボタン押下時に画面全体を `render()` で再生成するのではなく、対象となる行（row）の見た目だけを直接変更し、ユーザーに即座にフィードバックを返すようにします。

**変更内容:**
- アクション完了後、DOM上で対象のボタンを「完了状態（グレーアウトやチェックマーク）」に変更。
- 対象行の数量表示を操作後の値に更新。
- 処理の最後に `render()` を呼んでいた部分を削除し、上記DOM直接操作に差し替え。

---

## 防ぐべきリスクと安全対策

1. **バッチ上限（500件）への配慮**:
   - `writeBatch` は一度に500件までしか処理できませんが、仕入れや仕込みアクションは通常1〜数件の更新に留まるため、上限に達するリスクは極めて低いです。一括処理（`executeBatchPrepAction` 等）においてのみ、上限を超えないよう留意します。
2. **アーキテクチャの厳格な分離（React禁止の遵守）**:
   - かね将ポータルルート直下のファイル群であるため、引き続き React や外部ライブラリは使用せず、Vanilla JS による DOM API (`document.querySelector` 等) と Firebase SDK を用いて実装します。
3. **他機能との干渉**:
   - `m_store_items` 等のデータスキーマ（フィールド名など）は一切変更しません。純粋に書き込み方法（直列→バッチ）と描画タイミングの変更に留めるため、棚卸し機能や原価計算エンジンなど他の機能には干渉しません。

## レビュー依頼事項
上記計画にて実装を行いますが、内容に問題ないか（または追加で改修すべき点がないか）ご確認をお願いいたします。
